

-- select ingredients.name as ingredients, suppliers.name as suppliers
-- FROM ingredients
-- LEFT JOIN suppliers
-- on ingredients.supplier_id = suppliers.supplier_id
-- WHERE suppliers = 'Patty''s Premium Meats'

--Exercise 6
--select order_date, total_price, staff_id
--FROM orders
--WHERE staff_id = 1

--select orders.order_date, orders.total_price, orders.staff_id as role
--FROM orders
--LEFT JOIN staff
--on orders.staff_id = staff.staff_id
--WHERE staff.staff_id = 1

-- SELECT s.first_name, s.last_name, COUNT(o.order_id) AS total_orders
-- FROM staff AS s
-- JOIN orders AS o
-- ON s.staff_id = o.staff_id
-- GROUP BY s.staff_id, s.first_name,s.last_name;

--Exercise 8 Write a query to calculate the total revenue for each day of sales.
--SELECT date(o.order_date), sum(o.total_price) AS total_revenue_per_day
--FROM orders AS o
--GROUP BY date(o.order_date)
--ORDER BY date(o.order_date) DESC

--Exercise 9 Write a query to find out which menu item has been ordered the most.
/*SELECT mi.name, sum(oi.quantity) as total_sold
FROM orderitems AS oi
JOIN Menuitems as mi
on oi.item_id = mi.item_id
GROUP BY mi.name
ORDER BY total_sold DESC
LIMIT 1;*/

--Exercise 10 List All Ingredients for the 'Bacon Cheeseburger'
SELECT i.name, ri.quantity_needed, i.unit
FROM recipeitems AS ri
JOIN menuitems as mi on ri.item_id = mi.item_id
JOIN ingredients as i on ri.ingredient_id = i.ingredient_id
WHERE mi.name = 'Bacon Cheeseburger'